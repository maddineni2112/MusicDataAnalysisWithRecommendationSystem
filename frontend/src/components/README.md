# React Islands

The first milestone uses static JavaScript inside the Django shell so the app can run without a frontend build step. Vite React islands will replace or enhance those widgets in later dashboard/recommender milestones.
